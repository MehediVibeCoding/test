# এই রাউন্ডে কী কী যুক্ত/পরিবর্তন হলো

তোমার লাইভ repo (`MehediVibeCoding/test`)-এর সর্বশেষ ভার্সন ফ্রেশ ক্লোন করে তার
ওপরেই এই কাজ করা হয়েছে — আগের কোনো ফিচার/কনটেন্ট মোছা হয়নি, শুধু নিচের
জিনিসগুলো *যোগ* এবং *আপডেট* হয়েছে। রেফারেন্স হিসেবে তোমার ভাঙচুর
(`MehediVibeCoding/Vangcur`) রিপোর অ্যানিমেশন/গ্যালারি প্যাটার্ন থেকে আইডিয়া
নেওয়া হয়েছে, তবে কোনো কোড হুবহু কপি করা হয়নি — সাইটটার নিজস্ব sky-blue ডিজাইন
সিস্টেম অনুযায়ীই সব বানানো হয়েছে।

## ১. নতুন ৫টি সেকশন (সব জায়গায় ছবির জন্য প্লেসহোল্ডার রাখা আছে)

| কম্পোনেন্ট | কোথায় বসেছে | কী আছে |
|---|---|---|
| `RealClassroomShowcase.tsx` | AcademyFeatures ও Batches-এর মাঝে | বেন্টো-স্টাইল ফটো গ্রিড (৫টা স্লট), প্রতিটায় ক্যাপশন + "ছবি শীঘ্রই যুক্ত হবে" প্লেসহোল্ডার |
| `FarewellGallery.tsx` | Batches ও ClassDiary-এর মাঝে | ব্যাচ ফিল্টার (HSC 2025 / HSC 2026 / ক্লাসরুম মোমেন্টস) + ক্লিক করলে ফুলস্ক্রিন লাইটবক্স খোলে |
| `SuccessWall.tsx` | Testimonials-এর ঠিক পরে | "কৃতি শিক্ষার্থী দেয়াল" — নাম/GPA/কলেজের প্লেসহোল্ডার কার্ড |
| `FAQ.tsx` | CampusLocation ও AdmissionForm-এর মাঝে | ৫টা কমন প্রশ্নের অ্যাকর্ডিয়ন (ক্লিক করলে খোলে/বন্ধ হয়) |
| `StickyMobileBar.tsx` | পুরো সাইটে fixed (শুধু মোবাইলে দেখা যায়) | নিচে ফ্লোটিং বার — 🟢 WhatsApp/কল ও 🔵 ভর্তি ফরম বাটন। হিরো পার হলে দেখা যায়, ভর্তি ফরম সেকশনে পৌঁছালে নিজে থেকে লুকিয়ে যায় |

**ছবি বসানো:** প্রতিটা প্লেসহোল্ডারের ক্যাপশন/টেক্সট কোড কমেন্টে (`🔧`) লেখা আছে।
আসল ছবি হাতে পেলে শুধু ওই placeholder বক্সটার জায়গায় `next/image`-এর `<Image>`
বসিয়ে দিলেই লেআউট, গ্রিড, হোভার ইফেক্ট সব ঠিক থেকে যাবে — কাঠামো বদলাতে হবে না।

## ২. হিরো সেকশন — নতুন অ্যানিমেশন সিকোয়েন্স (`Hero.tsx`)

তুমি যেভাবে চেয়েছিলে ঠিক সেভাবেই সাজানো হয়েছে:
1. **মোবাইলে ছবি আগে আসে** — নিচ থেকে স্লাইড-আপ + ফেড-ইন (০.৭ সেকেন্ড)।
2. **এরপর টেক্সট আসে** — ছবির অ্যানিমেশন প্রায় শেষ হতে হতেই (৪২০ms দেরিতে) টেক্সট
   ব্লকগুলো একে একে (স্ট্যাগার) ফেড + স্লাইড-আপ হয়ে আসে।
3. **হেডলাইন "Md. Ahsan Ullah"** আলাদাভাবে **ডান থেকে বামে রিভিল** হয় (clip-path
   wipe অ্যানিমেশন — যেন পর্দা সরে গিয়ে লেখাটা বেরিয়ে আসছে)।
4. **স্ট্যাটাস কাউন্টার (৮+ বছর / ১০,০০০+ / ১০০%)** — নতুন `CountUp.tsx`
   কম্পোনেন্ট দিয়ে ০ থেকে গুণে গুণে আস্তে আস্তে থেমে টার্গেট সংখ্যায় আটকে যায়
   (ease-out কার্ভ), বাংলা অঙ্কেই দেখায় (`lib/bengaliNumerals.ts`)।
5. `prefers-reduced-motion` অন থাকলে কাউন্টার সরাসরি ফাইনাল সংখ্যা দেখায়,
   কোনো অ্যানিমেশন জোর করে চালানো হয় না।

## ৩. রিভিউ সেকশন সম্প্রসারণ (`Testimonials.tsx`)

আগের ৩টা ফিচার্ড টেস্টিমোনিয়াল কার্ড অপরিবর্তিত রাখা হয়েছে। তার ঠিক নিচে নতুন
একটা "আরও রিভিউ" ওয়াল যোগ হয়েছে (৮টা ডেমো এন্ট্রি) — Masonry-স্টাইল কলাম
গ্রিডে, প্রতিটায় শুধু **টেক্সট কোট + নাম + ব্যাচ/বছর + ভূমিকা** (শিক্ষার্থী নীল
ব্যাজ, অভিভাবক সোনালি ব্যাজ) — কোনো ছবি বা রেটিং স্টার ছাড়া, ঠিক যেভাবে বলেছিলে।
নতুন রিভিউ যোগ করতে চাইলে `Testimonials.tsx`-এর `MORE_REVIEWS` অ্যারেতে একটা
লাইন যোগ করলেই হবে।

## ৪. কোথায় নতুন সেকশনগুলো `page.tsx`-এ বসেছে

```
Navbar → Hero → About → AcademyFeatures
  → RealClassroomShowcase (NEW)
  → Batches
  → FarewellGallery (NEW)
  → ClassDiary → VideoGallery → BlogPreview
  → Testimonials (রিভিউ-ওয়াল সহ)
  → SuccessWall (NEW)
  → CampusLocation
  → FAQ (NEW)
  → AdmissionForm → Footer
  → StickyMobileBar (NEW, fixed — মোবাইলে সবসময় ওপরে ভাসে)
```

## ৫. যা এখনো করা হয়নি (তোমার কথামতো পরের ধাপে)

- আসল ছবি বসানো (সব জায়গায় শুধু প্লেসহোল্ডার রাখা হয়েছে, তুমি বলেছিলে ঠিক তাই)।
- ব্র্যান্ডিং/কালার ফাইন-টিউনিং — তুমি বলেছিলে "ইউআই রেডি করে ফেলো, ব্র্যান্ডের কাজ
  পরে করব" — তাই কালার প্যালেট সাইটের বিদ্যমান sky-blue সিস্টেম থেকেই নেওয়া
  হয়েছে, নতুন কোনো ব্র্যান্ড কালার/লোগো টাচ যোগ করা হয়নি।
- Backend/DB — কোনো ব্যাকএন্ড কাজ করা হয়নি (তুমি নিজেই বলেছিলে এই রাউন্ডে
  শুধু ফ্রন্টএন্ড)।

## ৬. যেভাবে বসাবে

জিপের ভেতরের path অনুযায়ী (`app/page.tsx`, `components/*.tsx`, `lib/*.ts`)
ফাইলগুলো তোমার repo-তে বসিয়ে/replace করে দাও। নতুন কোনো npm প্যাকেজ লাগেনি —
`motion` আগে থেকেই তোমার `package.json`-এ ছিল, সেটাই ব্যবহার হয়েছে।

`npm install && npm run build` (বা সরাসরি Vercel push) দিলেই কাজ করবে।
আমার টেস্ট এনভায়রনমেন্টে Google Fonts সরাসরি ব্লকড থাকায় প্রথমে সেটা মক করেই
পুরো `next build` (static generation সহ) সফলভাবে চালিয়ে যাচাই করা হয়েছে —
কোনো TypeScript/রানটাইম এরর নেই, Vercel-এ এটা নিয়ে সমস্যা হবে না।
